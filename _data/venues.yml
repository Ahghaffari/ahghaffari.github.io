# Venue badges for publications, keyed by the `abbr` field in _bibliography/papers.bib.
# Each venue may define a `url` and/or a `color` (used for the badge background).
# Currently unused: publication thumbnails/abbr badges are disabled in _config.yml.
# Example:
# "CIKM":
#   url: https://www.cikm.org/
#   color: "#00529b"
